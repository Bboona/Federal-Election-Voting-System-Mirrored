<%= form_with(model: vote) do |form| %>
  <% if vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vote.errors.count, "error") %> prohibited this vote from being saved:</h2>

      <ul>
        <% vote.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :preference %>
    <%= form.number_field :preference %>
  </div>

  <div class="field">
    <%= form.label :candidateID %>
    <%= form.number_field :candidateID %>
  </div>

  <div class="field">
    <%= form.label :ballotID %>
    <%= form.number_field :ballotID %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
